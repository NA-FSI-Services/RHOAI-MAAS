# BBR HTTPRoute for unified /v1/chat/completions routing to local Llama 3.1 8B.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: bbr-llama-3-1-8b-instruct
  namespace: llm
  labels:
    app.kubernetes.io/part-of: models-as-a-service
    maas.opendatahub.io/day7: lightspeed-maas
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: maas-default-gateway
      namespace: openshift-ingress
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /v1/chat/completions
          headers:
            - type: Exact
              name: X-Gateway-Model-Name
              value: llama-3-1-8b-instruct
      backendRefs:
        - group: ""
          kind: Service
          name: llama-3-1-8b-instruct-kserve-workload-svc
          port: 8000
          weight: 1
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplaceFullPath
              replaceFullPath: /v1/chat/completions
