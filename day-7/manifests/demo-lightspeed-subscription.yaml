# Dedicated subscription for OpenShift Lightspeed → MaaS gateway (local Llama 3.1 8B).
apiVersion: maas.opendatahub.io/v1alpha1
kind: MaaSSubscription
metadata:
  name: demo-openshift-lightspeed
  namespace: models-as-a-service
  labels:
    maas.opendatahub.io/day7: lightspeed-maas
  annotations:
    openshift.io/display-name: "Demo — OpenShift Lightspeed via MaaS"
    openshift.io/description: "Lightspeed consumes governed local Llama 3.1 8B inference through the MaaS gateway"
spec:
  priority: 22
  owner:
    groups:
      - name: system:authenticated
  tokenMetadata:
    organizationId: org-platform
    costCenter: CC-LIGHTSPEED-4001
    labels:
      team: openshift-lightspeed
      demo_persona: lightspeed
  modelRefs:
    - name: llama-3-1-8b-instruct
      namespace: llm
      tokenRateLimits:
        - limit: 50000
          window: 1m
